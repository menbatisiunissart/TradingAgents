{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "batch_runner",
      "type": "python",
      "request": "launch",
      "module": "cli.batch_runner",
      "args": [
        "config/batch_runner.yaml"
      ],
      "cwd": "${workspaceFolder}",
      "justMyCode": false
    }
  ]
}